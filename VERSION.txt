The Custom Made Canine v21.8.3 ONLINE TEST

Reports + A4 portrait hotfix:
- Payments report fixed: package rows now use PACKAGE-<id> because booking_packages has no package_ref field.
- Daily Sheet Notes column removed.
- Printed reports are constrained to A4 portrait.
- Print mode isolates the report sheet from the application shell to prevent an unnecessary blank/second page.
- Report tables use compact, report-specific columns and friendlier labels.
- CSV export still contains the full raw report data.
