The Custom Made Canine v21.9.16p ONLINE TEST

Course creation time-picker correction:
- Removed the custom/native clock popup from class start/end times.
- After Amy chooses the first course date, the app loads real availability in half-hour blocks.
- Existing private bookings, classes, unavailable time, recurring blocks, arena restrictions and working hours are excluded.
- Amy chooses Start first, then End from the remaining contiguous available block.
- Works with touch buttons on Android/iPhone and normal buttons on desktop.
- All future recurrence dates are still validated on save, so a later conflicting session cannot be created accidentally.
- Existing course editing remains locked once past sessions exist.

No database reset required.
