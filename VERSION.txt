The Custom Made Canine v21.9.2 ONLINE TEST

Small Phase 1 corrective update:
- Fixed the incorrect reviewsAdminView() reference; trainer admin now calls the existing reviewAdminView().
- Needs Attention shows “No pending reviews” as a disabled button when there are none.
- Book for client now uses a dedicated client picker. Selecting a client goes directly to the booking/package modal without opening the private client-detail sheet first.
- Normal Clients remains unchanged and still opens the full client record.

No payment, class, package-payment, refund, reschedule, calendar or media-storage logic changed.
