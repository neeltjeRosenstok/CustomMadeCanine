The Custom Made Canine v21.9.16o ONLINE TEST

Targeted correction:
- Scheduled Classes now shows a prominent standalone “Share training resource with this class” action directly under the selected course heading, rather than squeezing it into the Edit/Delete button row.
- Daily agenda now always displays real class sessions and bookings even when that date is marked as a non-working day or the event falls outside the currently configured working-hours window. This resolves cases where a purple class dot appeared in the month view but the class vanished in Daily View.
- Existing v21.9.16n resource sharing backend/client-profile support is preserved.

No database reset required.
