The Custom Made Canine v21.9.16m ONLINE TEST

Small patch on top of v21.9.16l:
- Keeps the complete Training Resource Library patch from v21.9.16l.
- Fixes the client “Cancel course enrolment” button. The course title is now looked up safely in app state instead of being injected into the inline button handler, which could break the click action when course names contained normal text/quotes.
- Cancellation still applies to the client's whole course enrolment / remaining sessions; the course itself continues for other clients and Amy then gets the refund decision.

No database reset required.
