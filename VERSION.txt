The Custom Made Canine v21.9.12b ONLINE TEST

Tiny class-cancellation refund hotfix:
- Trainer-initiated class cancellations now use the explicit status cancelled_by_trainer.
- Needs Attention no longer relies on a fragile list of cancellation statuses: every non-active class enrolment with payment_status=refund_pending is shown.
- The class refund endpoint uses the same rule, so Amy can handle any cancelled enrolment that is genuinely awaiting a refund decision.
- Existing older trainer-cancelled records using the legacy rejected status remain compatible.
- Class history now labels future trainer cancellations as “Cancelled by Amy”.
- The v21.9.12a large-iPad home alignment fix is preserved.

No database reset or migration is required.
