v21.9.13 CLASS REFUND CLEANUP

Replace:
- server.js
- package.json
- VERSION.txt
- GITHUB-UPDATE-INSTRUCTIONS.txt
- public/app.js
- public/styles.css
- public/index.html

Do NOT reset the database.

Test:
1. Open a class with several cancelled enrolments: all rows should align to the same left edge.
2. A refund_pending enrolment should have its action buttons inside its own highlighted row.
3. Enter 123456abcd as M-Pesa refund reference: it should normalize to 123456ABCD and be accepted if exactly 10 alphanumeric characters.
4. Enter an invalid code: the flow should keep the decision/amount and show the same code again for correction.
5. Test Full credit and Partial credit: no M-Pesa code requested, client credit balance increases, Needs Attention clears after decision.
6. Confirm old refunded/cancelled enrolments remain visible as quiet history rows.
