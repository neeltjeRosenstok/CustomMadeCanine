v21.9.2 small corrective update

Replace in GitHub:
- server.js
- package.json
- VERSION.txt
- public/app.js
- public/index.html

No database reset.

After Render redeploys, Ctrl+F5 and test:
1. Needs Attention with no reviews → “No pending reviews” is disabled.
2. Vaccination → Review record opens without a reviewsAdminView error.
3. Dashboard → Book for client → select client → booking screen opens directly, with no client-detail sheet shown first.
4. Dashboard → Clients → select client → full client detail still opens normally.
